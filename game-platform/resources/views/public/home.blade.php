@extends('layouts.public')
@section('title',$page->seo_title?:$page->title) @section('meta_description',$page->seo_description?:$page->excerpt) @section('canonical',$page->canonical_url?:url('/')) @section('og_title',$page->seo_title?:$page->title)
@section('content')
@php
$hero=$blocks->get('hero')?->payload??[];$releases=$blocks->get('releases')?->payload??[];$publishing=$blocks->get('publishing')?->payload??[];$community=$blocks->get('community')?->payload??[];
$slides=$featuredGames->take(4);$secondary=$featuredGames->skip(1)->take(2);
@endphp
<section class="hero-carousel" data-hero-carousel data-interval="5600" aria-roledescription="carousel" aria-label="Wyróżnione gry">
  <div class="hero-carousel-track">
    @forelse($slides as $slide)
      <article class="hero-slide tone-{{ $slide->accent }} {{ $loop->first?'is-active':'' }}" data-hero-slide aria-hidden="{{ $loop->first?'false':'true' }}" aria-label="{{ $loop->iteration }} z {{ $slides->count() }}: {{ $slide->title }}">
        <div class="container hero-slide-grid">
          <div class="hero-slide-copy" data-reveal="left">
            <div class="hero-slide-kicker"><span>Featured world</span><span>0{{ $loop->iteration }}</span></div>
            <h1 class="font-display">{{ $slide->title }}</h1>
            <p class="hero-slide-lead">{{ $slide->short_description }}</p>
            <div class="hero-actions"><a class="btn btn-primary" href="{{ route('games.show',$slide->slug) }}">Odkryj grę <span aria-hidden="true">↗</span></a><a class="btn btn-ghost" href="{{ route('games.index') }}">Pełny katalog</a></div>
            <div class="hero-meta"><span class="badge badge-success">{{ $slide->release_status->label() }}</span>@foreach($slide->platforms->take(3) as $platform)<span class="badge">{{ $platform->name }}</span>@endforeach</div>
          </div>
          <div class="hero-visual" data-depth-scene data-reveal="scale" aria-hidden="true">
            <div class="hero-visual-grid"></div><span class="hero-visual-number">WORLD / 0{{ $loop->iteration }}</span><div class="hero-visual-monogram">{{ strtoupper(substr($slide->title,0,2)) }}</div>
            <div class="hero-visual-caption"><span>{{ $slide->primaryGenre->first()?->name??$slide->genres->first()?->name??'Independent game' }}</span><strong>{{ $slide->release_date?->format('Y')??'TBA' }}</strong></div>
          </div>
        </div>
      </article>
    @empty
      <article class="hero-slide is-active" data-hero-slide aria-hidden="false"><div class="container hero-slide-grid"><div class="hero-slide-copy"><div class="hero-slide-kicker"><span>Featured world</span><span>01</span></div><h1 class="font-display">{{ data_get($hero,'title','Project Meridian') }}</h1><p class="hero-slide-lead">{{ data_get($hero,'promise',$page->excerpt) }}</p><div class="hero-actions"><a class="btn btn-primary" href="{{ route('games.index') }}">Odkryj gry <span aria-hidden="true">↗</span></a></div></div><div class="hero-visual" data-depth-scene aria-hidden="true"><div class="hero-visual-grid"></div><span class="hero-visual-number">WORLD / 01</span><div class="hero-visual-monogram">GS</div></div></div></article>
    @endforelse
  </div>
  <div class="hero-carousel-ui"><div class="container hero-carousel-ui-inner">
    <div class="hero-carousel-arrows"><button class="icon-btn carousel-arrow" type="button" data-hero-prev aria-label="Poprzedni slajd">←</button><button class="icon-btn carousel-arrow" type="button" data-hero-next aria-label="Następny slajd">→</button></div>
    <div class="hero-carousel-progress" aria-label="Wybierz slajd">@for($i=0;$i<max(1,$slides->count());$i++)<button class="hero-progress {{ $i===0?'is-active':'' }}" type="button" data-hero-progress aria-label="Przejdź do slajdu {{ $i+1 }}" aria-current="{{ $i===0?'true':'false' }}"></button>@endfor</div>
    <div class="hero-carousel-count" aria-live="polite"><strong data-hero-current>01</strong> / {{ str_pad((string)max(1,$slides->count()),2,'0',STR_PAD_LEFT) }}</div>
  </div></div>
</section>
<div class="motion-divider"></div>
<section class="section section-soft" id="worlds"><div class="container"><div class="section-head" data-reveal><div><p class="eyebrow">Selected worlds</p><h2 class="font-display">Nie jeden gatunek. Jeden standard jakości.</h2></div><a class="text-link" href="{{ route('games.index') }}">Pełny katalog →</a></div>
@if($featuredGames->isNotEmpty())<div class="editorial-strip"><a class="editorial-card" href="{{ route('games.show',$featuredGames->first()->slug) }}" data-reveal="left"><span class="editorial-card-index">01</span><div class="editorial-card-copy"><div class="badges"><span class="badge">{{ $featuredGames->first()->primaryGenre->first()?->name??'Gra' }}</span><span class="badge badge-success">{{ $featuredGames->first()->release_status->label() }}</span></div><h3 class="font-display">{{ $featuredGames->first()->title }}</h3><p>{{ $featuredGames->first()->short_description }}</p></div></a><div class="editorial-stack">@foreach($secondary as $game)<a class="editorial-card small" href="{{ route('games.show',$game->slug) }}" data-reveal="right"><span class="editorial-card-index">0{{ $loop->iteration+1 }}</span><div class="editorial-card-copy"><span class="badge">{{ $game->primaryGenre->first()?->name??'Gra' }}</span><h3 class="font-display">{{ $game->title }}</h3><p>{{ $game->release_status->label() }}</p></div></a>@endforeach</div></div>@endif
<div class="genre-row" style="margin-top:22px"><a class="chip is-active" href="{{ route('games.index') }}">Wszystkie</a><a class="chip" href="{{ route('games.index',['genre'=>'rpg']) }}">RPG</a><a class="chip" href="{{ route('games.index',['genre'=>'strategia']) }}">Strategia</a><a class="chip" href="{{ route('games.index',['genre'=>'psychological-horror']) }}">Horror</a><a class="chip" href="{{ route('games.index',['genre'=>'puzzle']) }}">Logiczne</a></div></div></section>
<section class="section"><div class="container"><div class="section-head" data-reveal><div><p class="eyebrow">Release radar</p><h2 class="font-display">Co nadchodzi</h2></div></div><div class="release-rail">@forelse($upcomingGames as $game)<article class="release-card" data-reveal="scale"><span class="release-date">{{ $game->release_date?->format('m.Y')??'TBA' }}</span><h3><a href="{{ route('games.show',$game->slug) }}">{{ $game->title }}</a></h3><p>{{ $game->release_status->label() }} · {{ $game->short_description }}</p></article>@empty @foreach(data_get($releases,'items',[]) as $item)<article class="release-card"><span class="release-date">{{ $item['date'] }}</span><h3>{{ $item['title'] }}</h3><p>{{ $item['copy'] }}</p></article>@endforeach @endforelse</div></div></section>
<section class="section section-soft"><div class="container publisher-panel" data-reveal="scale"><div><p class="eyebrow">For developers</p><h2 class="font-display">Wydaj z nami bez utraty własnego głosu.</h2><p>{{ data_get($publishing,'copy','Łączymy produkcję, marketing, dystrybucję i wsparcie wydawnicze z przejrzystym procesem decyzyjnym.') }}</p><a class="btn btn-primary" href="{{ url(data_get($publishing,'cta_url','/wydaj-z-nami')) }}">Poznaj proces <span aria-hidden="true">↗</span></a></div><div class="process-grid">@foreach(data_get($publishing,'steps',[]) as $step)<article class="process-step" data-motion-card><span>{{ $step['number'] }}</span><h3>{{ $step['title'] }}</h3><p>{{ $step['copy'] }}</p></article>@endforeach</div></div></section>
<section class="section"><div class="container"><div class="section-head" data-reveal><div><p class="eyebrow">Journal</p><h2 class="font-display">Z produkcji i ze studia</h2></div><a class="text-link" href="{{ route('news.index') }}">Wszystkie aktualności →</a></div><div class="news-grid">@forelse($latestPosts as $post)<article class="news-card" data-reveal="scale"><div class="news-cover"><span>{{ $post->category?->name??'Studio' }}</span></div><div class="news-body"><div class="news-meta"><span>{{ optional($post->published_at)->format('d.m.Y') }}</span><span>{{ $post->reading_minutes }} min</span></div><h3 class="font-display"><a href="{{ route('news.show',$post->slug) }}">{{ $post->title }}</a></h3><p>{{ $post->excerpt }}</p><a class="text-link" href="{{ route('news.show',$post->slug) }}">Czytaj →</a></div></article>@empty<div class="empty-state">Brak opublikowanych aktualności.</div>@endforelse</div></div></section>
<section class="section"><div class="container community-band" data-reveal="scale"><div><p class="eyebrow">Community</p><h2 class="font-display">{{ data_get($community,'title','Bądź bliżej premier') }}</h2><p>{{ data_get($community,'copy') }}</p></div><div class="community-actions"><a class="btn btn-secondary" href="{{ data_get($community,'discord_url','#') }}">Discord</a><a class="btn btn-ghost" href="{{ route('news.index') }}">Devlog</a></div></div></section>
@endsection