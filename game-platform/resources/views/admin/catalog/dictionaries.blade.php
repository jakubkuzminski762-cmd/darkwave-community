@extends('layouts.public') @section('title','Słowniki katalogu') @section('content')<section class="section"><div class="container"><div class="section-head"><div><p class="eyebrow">Administracja · katalog</p><h1 class="font-display">Słowniki</h1></div><a class="btn btn-ghost" href="{{ route('admin.games.index') }}">← Gry</a></div>@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif<div class="dictionary-grid">@foreach([['genre','Gatunki',$genres],['tag','Tagi',$tags],['platform','Platformy',$platforms],['mode','Tryby gry',$modes],['accessibility','Dostępność',$accessibility]] as [$type,$label,$items])<section class="panel panel-pad"><h2>{{ $label }}</h2><div class="dictionary-items">@foreach($items as $item)<span class="badge">{{ $item->name }}</span>@endforeach</div><form method="post" action="{{ route('admin.games.dictionaries.store') }}" class="form-grid">@csrf<input type="hidden" name="type" value="{{ $type }}"><label class="field"><span class="field-label">Nazwa</span><input class="field-control" name="name" required></label><label class="field"><span class="field-label">Slug</span><input class="field-control" name="slug" required></label>@if($type==='genre')<label class="field"><span class="field-label">Grupa</span><input class="field-control" name="group_name"></label>@endif<button class="btn btn-primary">Dodaj</button></form></section>@endforeach</div></div></section>@endsection