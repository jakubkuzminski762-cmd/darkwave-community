@extends('layouts.public') @section('title','CMS | '.config('app.name')) @section('content')<section class="page-hero"><div class="container"><p class="eyebrow">Administracja</p><h1 class="font-display">CMS</h1><p class="page-lead">Strony, aktualności i media. Dostęp wyłącznie z uprawnieniem redaktora.</p></div></section><section class="section"><div class="container admin-grid"><div class="panel panel-pad"><h2>Strony</h2>@foreach($pages as $page)<div class="admin-row"><div><strong>{{ $page->title }}</strong><small>{{ $page->status->label() }}</small></div><a class="btn btn-ghost" href="{{ route('admin.cms.pages.edit',$page) }}">Edytuj</a></div>@endforeach</div><div class="panel panel-pad"><h2>Aktualności</h2>@foreach($posts as $post)<div class="admin-row"><div><strong>{{ $post->title }}</strong><small>{{ $post->status->label() }}</small></div><a class="btn btn-ghost" href="{{ route('admin.cms.posts.edit',$post) }}">Edytuj</a></div>@endforeach</div><div class="panel panel-pad"><h2>Media</h2>@forelse($media as $item)<div class="admin-row"><div><strong>{{ $item->name }}</strong><small>{{ $item->mime_type??'media' }}</small></div></div>@empty<p class="muted">Brak mediów.</p>@endforelse</div></div></section>@endsection
