document.addEventListener('DOMContentLoaded',()=>{const qs=(s,c=document)=>c.querySelector(s);const qsa=(s,c=document)=>[...c.querySelectorAll(s)];const menu=qs('[data-mobile-menu]');const openMenu=qs('[data-mobile-open]');const closeMenu=qs('[data-mobile-close]');const setMenu=open=>{if(!menu||!openMenu)return;menu.classList.toggle('is-open',open);openMenu.setAttribute('aria-expanded',String(open));if(open&&closeMenu)closeMenu.focus()};openMenu?.addEventListener('click',()=>setMenu(true));closeMenu?.addEventListener('click',()=>setMenu(false));qsa('[data-mobile-link]').forEach(a=>a.addEventListener('click',()=>setMenu(false)));const modal=qs('.modal-backdrop');const openModal=qs('.hero-actions .btn-ghost');const modalX=modal?qs('.modal-head .icon-btn',modal):null;const modalCancel=modal?qs('.modal-actions .btn-ghost',modal):null;const modalConfirm=modal?qs('.modal-actions .btn-primary',modal):null;let lastFocus=null;const setModal=open=>{if(!modal)return;if(open){lastFocus=document.activeElement;modal.classList.add('is-open');document.body.style.overflow='hidden';setTimeout(()=>modalX?.focus(),0)}else{modal.classList.remove('is-open');document.body.style.overflow='';lastFocus?.focus?.()}};openModal?.addEventListener('click',()=>setModal(true));modalX?.addEventListener('click',()=>setModal(false));modalCancel?.addEventListener('click',()=>setModal(false));modal?.addEventListener('click',e=>{if(e.target===modal)setModal(false)});modal?.addEventListener('keydown',e=>{if(e.key!=='Tab')return;const f=qsa('button,[href],input,textarea,select,[tabindex]:not([tabindex="-1"])',modal).filter(el=>!el.disabled);if(!f.length)return;const first=f[0],last=f[f.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}});const toast=qs('.toast');const toastOpen=qs('.hero-actions .btn-secondary');const toastClose=toast?qs('.icon-btn',toast):null;let toastTimer;const showToast=()=>{if(!toast)return;toast.classList.add('is-open');clearTimeout(toastTimer);toastTimer=setTimeout(()=>toast.classList.remove('is-open'),5000)};toastOpen?.addEventListener('click',showToast);toastClose?.addEventListener('click',()=>toast?.classList.remove('is-open'));modalConfirm?.addEventListener('click',()=>{setModal(false);showToast()});document.addEventListener('keydown',e=>{if(e.key!=='Escape')return;if(modal?.classList.contains('is-open'))setModal(false);else if(menu?.classList.contains('is-open'))setMenu(false)});qsa('.chip').forEach(chip=>chip.addEventListener('click',()=>{qsa('.chip').forEach(c=>c.setAttribute('aria-pressed','false'));chip.setAttribute('aria-pressed','true')}));const pitch=qs('#demo-pitch');const count=qs('.field-count span');if(pitch&&count){const update=()=>count.textContent=String(pitch.value.length);pitch.addEventListener('input',update);update()}});