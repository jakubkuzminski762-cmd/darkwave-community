document.addEventListener('DOMContentLoaded',()=>{const q=(s,c=document)=>c.querySelector(s),qa=(s,c=document)=>[...c.querySelectorAll(s)];const menu=q('[data-mobile-menu]'),open=q('[data-mobile-open]'),close=q('[data-mobile-close]');const setMenu=v=>{if(!menu||!open)return;menu.classList.toggle('is-open',v);open.setAttribute('aria-expanded',String(v));if(v)close?.focus()};open?.addEventListener('click',()=>setMenu(true));close?.addEventListener('click',()=>setMenu(false));qa('[data-mobile-link]').forEach(a=>a.addEventListener('click',()=>setMenu(false)));document.addEventListener('keydown',e=>{if(e.key==='Escape')setMenu(false)});qa('.chip').forEach(chip=>chip.addEventListener('click',()=>{if(chip.tagName!=='BUTTON')return;qa('.chip').forEach(c=>c.tagName==='BUTTON'&&c.setAttribute('aria-pressed','false'));chip.setAttribute('aria-pressed','true')}));if(!matchMedia('(prefers-reduced-motion: reduce)').matches&&'IntersectionObserver'in window){const io=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');io.unobserve(entry.target)}}),{threshold:.12});qa('[data-reveal]').forEach(el=>io.observe(el))}else qa('[data-reveal]').forEach(el=>el.classList.add('is-visible'));});
