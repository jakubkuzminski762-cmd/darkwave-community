document.addEventListener('DOMContentLoaded',()=>{const qs=(s,c=document)=>c.querySelector(s);const qsa=(s,c=document)=>[...c.querySelectorAll(s)];const menu=qs('[data-mobile-menu]');const openMenu=qs('[data-mobile-open]');const closeMenu=qs('[data-mobile-close]');const setMenu=(open)=>{if(!menu||!openMenu)return;menu.classList.toggle('is-open',open);openMenu.setAttribute('aria-expanded',String(open));if(open&&closeMenu)closeMenu.focus();if(!open)openMenu.focus()};openMenu?.addEventListener('click',()=>setMenu(true));closeMenu?.addEventListener('click',()=>setMenu(false));qsa('[data-mobile-link]').forEach(a=>a.addEventListener('click',()=>setMenu(false)));const modal=qs('[data-modal]');let lastFocus=null;const modalOpeners=qsa('[data-modal-open]');const modalClosers=qsa('[data-modal-close]');const setModal=(open)=>{if(!modal)return;if(open){lastFocus=document.activeElement;modal.classList.add('is-open');document.body.style.overflow='hidden';setTimeout(()=>qs('[data-modal-close]',modal)?.focus(),0)}else{modal.classList.remove('is-open');document.body.style.overflow='';lastFocus?.focus?.()}};modalOpeners.forEach(b=>b.addEventListener('click',()=>setModal(true)));modalClosers.forEach(b=>b.addEventListener('click',()=>setModal(false)));modal?.addEventListener('click',e=>{if(e.target===modal)setModal(false)});modal?.addEventListener('keydown',e=>{if(e.key!=='Tab')return;const f=qsa('button,[href],input,textarea,select,[tabindex]:not([tabindex="-1"])',modal).filter(el=>!el.disabled);if(!f.length)return;const first=f[0],last=f[f.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}});const toast=qs('[data-toast]');let toastTimer;const showToast=()=>{if(!toast)return;toast.classList.add('is-open');clearTimeout(toastTimer);toastTimer=setTimeout(()=>toast.classList.remove('is-open'),5000)};qsa('[data-toast-open]').forEach(b=>b.addEventListener('click',showToast));qs('[data-toast-close]')?.addEventListener('click',()=>toast?.classList.remove('is-open'));document.addEventListener('keydown',e=>{if(e.key!=='Escape')return;if(modal?.classList.contains('is-open'))setModal(false);else if(menu?.classList.contains('is-open'))setMenu(false)});qsa('.chip').forEach(chip=>chip.addEventListener('click',()=>{qsa('.chip').forEach(c=>c.setAttribute('aria-pressed','false'));chip.setAttribute('aria-pressed','true')}));qsa('[data-count-source]').forEach(el=>{const target=qs(`[data-count-target="${el.dataset.countSource}"]`);const update=()=>{if(target)target.textContent=String(el.value.length)};el.addEventListener('input',update);update()})});