document.addEventListener('DOMContentLoaded',()=>{const openModal=(modal)=>{if(!modal)return;modal.classList.add('is-open');modal.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'};const closeModal=(modal)=>{if(!modal)return;modal.classList.remove('is-open');modal.setAttribute('aria-hidden','true');document.body.style.overflow=''};const trailer=document.querySelector('[data-trailer-modal]');document.querySelector('[data-trailer-open]')?.addEventListener('click',()=>{const frame=trailer?.querySelector('[data-trailer-frame]');if(frame&&!frame.src)frame.src=frame.dataset.src;openModal(trailer)});document.querySelector('[data-trailer-close]')?.addEventListener('click',()=>{const frame=trailer?.querySelector('[data-trailer-frame]');if(frame)frame.src='';closeModal(trailer)});const gallery=document.querySelector('[data-gallery-modal]');document.querySelectorAll('[data-gallery-item]').forEach(item=>item.addEventListener('click',()=>{const title=gallery?.querySelector('[data-gallery-title]');const art=gallery?.querySelector('[data-gallery-art]');if(title)title.textContent=item.dataset.galleryTitle||'';if(art)art.className='gallery-modal-art '+[...item.classList].find(c=>c.startsWith('tone-'));openModal(gallery)}));document.querySelector('[data-gallery-close]')?.addEventListener('click',()=>closeModal(gallery));[trailer,gallery].forEach(modal=>modal?.addEventListener('click',e=>{if(e.target===modal)closeModal(modal)}));document.addEventListener('keydown',e=>{if(e.key==='Escape'){closeModal(trailer);closeModal(gallery)}})});
